name: test-gh-actions-release

on:
  release:
    types: [ published ]
jobs:
  test-release:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: |
          echo ${{ github.event }}