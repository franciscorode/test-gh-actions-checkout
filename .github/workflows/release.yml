name: test-gh-actions-release

on:
  release:
    types: [published]
    tags-ignore:
      - v1.*
jobs:
  test-release:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: |
          echo "release"